// URL del directorio central (CONECTOR). En desarrollo, la IP de tu maquina
// en la red local (no "localhost": el celular/emulador no la resuelve igual).
export const CONECTOR_URL = 'http://localhost:4000/api';
